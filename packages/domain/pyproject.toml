[project]
name = "kb-domain"
version = "0.1.0"
description = "Shared domain entities and value objects"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/kb_domain"]
