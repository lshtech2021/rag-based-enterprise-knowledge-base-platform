# Tasks: identity

**Status:** Complete

- [x] `kb-identity` package: Principal, Role, ports, DevBypass, HmacJwt
- [x] FastAPI deps in BFF: require_principal / require_roles
- [x] BFF settings + `/v1/me` + protect `/v1/query`
- [x] Tests + quality gates
